import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => {
    return(
        <header>
            <div>
                Redux App
            </div>
        </header>
    )
}

export default Header;